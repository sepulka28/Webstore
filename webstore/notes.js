
{...register("email", {required: true, message: "This field is required"} )}
// {...register("password", {required: true, minLength:
//   {value: 8, message: "The password must contain at least 8 digits"} }) }

// onSubmit={handleSubmit(onSubmit)}

// ({
  //   resolver: zodResolver(validateData)
  // });